<%= form_for @exercise do |f| %>
  <div class="form-group"><%= f.label :name %><%= f.text_field :name, class: 'form-control' %></div>
  <div class="form-group"><%= f.label :detail %><%= f.text_field :detail, class: 'form-control' %></div>
  <div class="form-group"><%= f.label :duration %><%= f.text_field :duration, class: 'form-control' %></div>
  <% if @workout %>
    <%= hidden_field :workout_ids, value: @workout.id %>
  <% end %>
  <div class="form-group"><%= f.submit submit, class: 'btn btn-default' %></div>
<% end %>
