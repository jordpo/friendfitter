<%= form_for @exercise do |f| %>
  <%= f.label :name %><%= f.text_field :name %>
  <%= f.label :detail %><%= f.text_field :detail %>
  <%= f.label :duration %><%= f.text_field :duration %>
  <h3>Workouts:</h3>
  <%= collection_check_boxes(:exercise, :workout_ids, Workout.all, :id, :name) %>
  <%= f.submit %>
<% end %>
